# Turn the engines field into a hard failure instead of a warning.
#
# Without this, npm only prints EBADENGINE and carries on, so an unsupported
# Node version surfaces much later as a native build crash — on Node 20 that
# was "npm error Exit handler never called!", which says nothing about the
# actual cause. With it, the install stops immediately and names the version.
engine-strict=true
